@*model declaration (notice model is not Capitalized)*@
@model BethanysPieShop.ViewModels.HomeViewModel

<h2>@Model.Title</h2>

@*Model (Capitilized) is a property containing the a collection of pies sent as argument in the method Controller.View(pies)*@
@foreach(var pie in Model.Pies)
{
<div>
    <h2>@pie.Name</h2>
    <h3>@pie.Price.ToString()</h3>
</div>
}
